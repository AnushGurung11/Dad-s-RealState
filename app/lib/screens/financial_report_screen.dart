import 'package:flutter/material.dart';

/// Placeholder for Financial Report screen - will be fleshed out in section 11.
/// For section 10, it just needs to exist as a tab.
class FinancialReportScreen extends StatelessWidget {
  const FinancialReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Financial Report'));
  }
}
